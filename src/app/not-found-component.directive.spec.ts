import { NotFoundComponentDirective } from './not-found-component.directive';

describe('NotFoundComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new NotFoundComponentDirective();
    expect(directive).toBeTruthy();
  });
});
